// 各工具頁的說明文件（內容同步自 docs/guides/*.md，供頁面「幫助」浮動視窗使用）
// 支援的 Markdown 子集：## / ### 標題、- 清單、1. 有序清單、> 引言、表格、**粗體**、`程式碼`

export const HELP_DOCS = {
  'excel-merge-tool': {
    title: '庫存表工具 使用說明',
    content: `## 功能說明

將含有多個倉庫區塊的 Excel 庫存來源檔，整併成一份制式格式的庫存表。

> 本工具**不需要上傳模板**，輸出欄位已固定寫在程式中（頁面上的「輸出格式（制式模板）」區塊即為範例）。

## 使用步驟

1. 上傳**來源檔**（含各倉庫資料的 Excel，副檔名 \`.xlsx\`）
2. **可一次選多個檔案，也可以分次上傳累加**。典型用法：主庫存檔（總倉／電商倉／平台總倉）＋ 展威檔各一份
3. 同檔名只會收一次，重複選到會跳過並提示，避免同一份庫存被加兩次；上傳清單可逐檔「移除」
4. 系統自動辨識每個檔案裡的倉庫區塊（總倉 / 官網 / 平台 / 展威），可點「預覽表格」確認辨識結果
5. 點擊**產生庫存表**，再點擊**匯出Excel**

檔名規則：單檔為 \`庫存表_<來源檔名>_<YYMMDD>.xlsx\`；多檔為 \`庫存表_合併<N>檔_<YYMMDD>.xlsx\`。

## 來源檔格式說明

- 只讀取第一個工作表。
- 來源檔是「商品各倉庫存數量查詢」報表，統計方式為**倉庫分頁**：每個倉庫一段，開頭是 \`倉庫 :<倉庫名稱>\`，接著是欄位名稱列，然後資料列，最後一列是 \`數量小計\`。
- 區塊類型依**倉庫名稱**判斷，順序如下（先命中先算）：

| 倉庫名稱含 | 歸類 | 實際例子 |
|---|---|---|
| 展威麗嬰房(平台總倉) | 展威 | 展威麗嬰房(平台總倉) |
| 平台 / 平臺 | 平台 | 展宇麗嬰(平台總倉) |
| 電商 / 官網 | 官網 | 展宇麗嬰(哈瓦仕電商倉) |
| 其他（未知） | 總倉 | 展宇麗嬰房總倉 |

- 順序不可調換：「展威麗嬰房(平台總倉)」同時含「平台」與「總倉」，不先攔會被判成平台。
- \`ELLE\`、\`誠品\` 兩欄目前保留在輸出格式中，但尚未有對應的倉庫判斷規則，會固定留白。
- 標題列之後、含「商品代號」「商品名稱」的那列視為欄位名稱列，其後的資料列才會被讀取。
- 含「小計」「合計」「數量」的列視為統計列，不計入資料。

## 來源欄位對應

| 來源欄 | 用途 |
|---|---|
| A | 商品代號 |
| B | 商品名稱 |
| L | 可售量（依區塊填入 總倉／官網／平台／展威） |
| M | 含稅定價 |
| N | 年度 |
| P | 尺寸名稱 |

## 輸出格式

單一 Excel，分頁名稱 \`庫存匯總\`，欄位順序：

\`商品代號｜商品名稱｜貨號｜尺寸名稱｜年度｜含稅定價｜總倉｜官網｜平台｜展威｜ELLE｜誠品｜備註\`

- **貨號**：商品代號需剛好含 **2 個 \`-\`** 才產生；\`SG\` 開頭取前 12 字，其餘取前 13 字；不符則留白。
- **數量為 0 時留白**（與紙本庫存表格式一致）。
- **備註**：一律留白，供人工填寫。
- 排序：年度**倒序**，同年度再依商品代號自然排序。

## 注意事項

- 同一個商品代號在多個區塊（含跨檔案）出現時，會合併成同一列，各倉庫數量分別填入對應欄位。
- **多個倉庫落在同一個輸出欄位時會相加**。例：「展威麗嬰房」與「展威麗嬰房(平台總倉)」都算展威。
- 同一個倉庫在檔案裡重複出現（分頁）不會重複累加，以最後一次的數量為準。
- 未知倉庫類型一律歸類為**總倉**。
- 支援格式：\`.xlsx\``,
  },

  'daily-shipping-combine': {
    title: '每日出貨合併工具 使用說明',
    content: `## 功能說明

將每日從各出貨通路（宅配、7-11、全家）下載的 CSV 訂單資料合併，並累積至統一的 Excel 報表。

## 使用步驟

1. （選用）上傳**既有報表**（前次匯出的 Excel），讓系統自動延續舊資料
2. 上傳**新的訂單 CSV**（可多檔同時上傳）
3. 系統依 Tags 欄位自動分類至宅配 / 7-11 / 全家
4. 點擊**下載**取得合併後的 Excel

## 輸入檔案格式

- 格式：CSV（逗號分隔）
- 必要欄位：\`Tags\`（用於判斷通路）、\`Payment ID\`（用於去重）、\`Name\`（母單號碼）
- 通路判斷邏輯：

| Tags 內容 | 分頁 |
|---|---|
| 含「宅配」 | 宅配 |
| 含「全家」 | 全家 |
| 含「7-11」或「711」 | 7-11 |

## 去重機制

- 以 \`Payment ID\` 判斷是否重複
- 子單（無獨立識別碼）以母單 + 子單整批判斷，相同批次不重複新增

## 輸出格式

- Excel，含分頁：\`宅配\`、\`7-11\`、\`全家\`、\`匯總\`
- 另有 \`未分類\` 分頁：\`Tags\` 判不出通路的訂單會落在這裡（沒有這類訂單時不會產生此分頁）
- 各分頁資料依日期降序排列（最新在上）

## 注意事項

- 每次上傳前建議先上傳既有報表，避免資料重複
- 若未上傳既有報表，本次上傳資料將作為全新報表`,
  },

  'count-shipping-subtotal': {
    title: '每日業績統計工具 使用說明',
    content: `## 功能說明

讀取出貨訂單 CSV，**依日期**計算各通路（宅配、7-11、全家）的業績、訂單數、雙數，並扣除退貨。

## 使用步驟

1. （選用）上傳**既有統計報表**（前次匯出的 Excel）
2. 上傳**新的訂單 CSV**（可多檔）
3. 系統依通路分類並計算業績
4. 點擊**下載**取得統計報表

## 輸入檔案格式

- 格式：CSV
- 通路分類方式同每日出貨合併工具（依 \`Tags\` 欄位：宅配 / 全家 / 7-11；判不出的歸「未分類」）
- 以 \`Name\`（母單號碼）分組，母單決定通路與退貨狀態

## 業績計算說明

- 依**日期**彙總各通路的業績、訂單數、雙數，並區分有／無運費
- 退貨訂單另計 \`退貨訂單數\`、\`退貨總雙數\`、\`退貨業績\`

## 輸出格式

Excel，分頁如下：

- 各通路分頁：\`宅配\`、\`7-11\`、\`全家\`（保留原始訂單列）
- \`未分類\`（僅在有無法分類的訂單時產生）
- \`統計\`：欄位為 \`日期｜總業績｜總訂單數｜總雙數｜退貨訂單數｜退貨總雙數｜退貨業績\`，後接各通路的 \`有運費｜無運費｜總雙數｜業績｜平均金額\`（宅配、7-11、全家各一組）

## 注意事項

- 退貨資料須包含在上傳的 CSV 中，系統依母單狀態自動識別`,
  },

  'month-shipping-count': {
    title: '月份業績統計工具 使用說明',
    content: `## 功能說明

按**月份**統計業績（不分通路），並計算 AUP、UPT、AOV 等指標，可累積至既有的月份統計報表。

## 使用步驟

1. （選用）上傳**既有月份統計報表**（前次匯出的 Excel，需含 \`月份統計\` 分頁）
2. 上傳**新的訂單 CSV**
3. 系統自動統計各月份業績
4. 點擊**下載**取得更新後的統計報表，檔名 \`月份業績統計_<YYYY-MM-DD>.xlsx\`

## 輸入檔案格式

- 格式：CSV
- 日期欄位用於判斷月份歸屬
- 統計方式**不區分來源通路**

## 輸出格式

Excel，分頁 \`月份統計\`，欄位：

\`月份｜總業績｜總訂單數｜總雙數｜淨業績｜淨訂單數｜淨雙數｜退貨訂單數｜退貨總雙數｜退貨業績｜AUP｜UPT｜AOV\`

- 淨值＝總值扣除退貨
- 依月份**降序**排列（最新在上）

## 注意事項

- 上傳既有報表可累積歷史資料，同月份會以新資料合併更新`,
  },
};

export default HELP_DOCS;
